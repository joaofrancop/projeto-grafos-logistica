#include "logistica.h"
#include <stdio.h>

int* dijkstra(Grafo* grafo, int origem) {
    // Implementação do Dijkstra aqui
    return NULL;
}

int* a_star(Grafo* grafo, int origem, int destino, int (*heuristica)(int, int)) {
    // Implementação do A* aqui
    return NULL;
}
